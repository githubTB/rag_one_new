# ── Web 服务 ──────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart
httpx                       # 异步调用 Ollama

# ── Embedding ────────────────────────────────────────────────
sentence-transformers>=2.7.0
torch                       # sentence-transformers 依赖

# ── 向量库 ────────────────────────────────────────────────────
pymilvus>=2.4.0

# ── 文档解析 ──────────────────────────────────────────────────
pymupdf>=1.24.0             # PDF 解析（比 pypdf 结构更完整）
pdfplumber>=0.10.0          # PDF 表格提取
python-docx>=1.1.0          # Word 文档
openpyxl>=3.1.0             # Excel 文档

# ── OCR（二选一，推荐 PaddleOCR）──────────────────────────────
# PaddleOCR（中文首选）：
#   pip install paddlepaddle paddleocr
#
# Tesseract（轻量备选）：
#   apt-get install tesseract-ocr tesseract-ocr-chi-sim
#   pip install pytesseract Pillow

Pillow                      # 图像处理（OCR 预处理必需）

# ── LangChain（仅保留 Chunk.to_langchain_doc 兼容层）──────────
langchain-core>=0.2.0

# ── 工具 ──────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv

# ── 分词 ─────────────────────────────────────────────────────
jieba                       # 中文分词，用于关键词检索