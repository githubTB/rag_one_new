PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True

LLM_OCR_TIMEOUT=120
LLM_OCR_MAX_PX=1600

# 当前你的情况：不用 LLM，只用传统引擎
OCR_ENGINES=paddle,tesseract

# 如果 Paddle 也没装，只用 Tesseract
OCR_ENGINES=tesseract

# 想试 LLM 放最后兜底
OCR_ENGINES=paddle,tesseract,llm